#include<stdio.h>
#define N 5;
int s[N];
void push()
{
   int x;
   printf("Enter the element");
   scanf("%d",&x);
   if(top>N-1)
   {
      printf("Stack overflow");
      }
   else {
      top=top+1;
      s[top]=x;
   }
}
void display()
{
   int i;
   for(i=top;i>=0;i--)
   {
      printf("element%d",&[i]);
   }
}
void main()
{
   push();
   display();
}
